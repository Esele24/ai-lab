# Everything else is Python standard library: http.server, sqlite3, json, base64, re.
# Versions pinned to what was verified locally on 2026-08-11 (Python 3.13.14).
numpy==2.5.1
pandas==3.0.3
pypdf==6.14.2
python-docx==1.2.0
requests==2.34.2
